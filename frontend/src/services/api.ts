// This file is intentionally minimal.
// All LLM config storage uses localStorage (configStore.ts).
// All Ollama API calls go directly to http://localhost:11434 (llmService.ts).
// No backend server is required.

export {}
